import React from 'react';
import styles from './Footer.module.css'

const Footer = () => {
    return (
        <div>
            <h3 className={styles.title}>This is footer</h3>
        </div>
    );
};

export default Footer;